import React from 'react';
import ReactDOM from 'react-dom/client';
import BiodataDiri from './BiodataDiri';
import './custom.css'; // Import file CSS custom

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BiodataDiri />
  </React.StrictMode>
);